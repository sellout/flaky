# Emacs Lisp Nix flake template

This template uses Eldev to build elisp packages.

## TODO

- don’t let it pull down its own dependencies, use Nix
- package zipped source and byte-compiled files, no bare source
